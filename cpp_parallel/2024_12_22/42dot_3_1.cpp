#include <iostream>
#include <vector>
#include <string>

using namespace std;

vector<int> solution(vector<int> &black_caps) {
    int n = black_caps.size();
    vector<int> answer(n, 0);
    return answer;
}